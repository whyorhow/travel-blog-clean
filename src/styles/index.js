/**
 * Styles — Design System Exports
 * 
 * Usage:
 *   import { tokens, tw, styles } from '../styles';
 *   
 *   <h1 className={tw.hero}>{title}</h1>
 *   <p className={tw.body}>{content}</p>
 *   
 *   OR use tokens for calculations:
 *   const spacing = tokens.spacing.section;
 */

export { tokens, tw, styles } from './tokens';
export { default } from './tokens';
