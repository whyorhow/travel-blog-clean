import templateMeta from "../../../assets/filmstripTemplates/5HCustom01.json";

/**
 * Strip — baked Illustrator chrome for 5-frame wall previews.
 * Frame slots from green registration paths in the SVG.
 */
export const FILMSTRIP_5H_CUSTOM_01 = templateMeta;
