import templateMeta from "../../../assets/filmstripTemplates/3HCustom01.json";

/**
 * Strip — baked Illustrator chrome for 3-frame wall previews.
 * Frame slots from green registration paths in the SVG.
 */
export const FILMSTRIP_3H_CUSTOM_01 = templateMeta;
