import templateMeta from "../../../assets/filmstripTemplates/6HCustom01.json";

/**
 * Strip — baked Illustrator chrome for 6-frame wall previews.
 * Frame slots from green registration paths in the SVG.
 */
export const FILMSTRIP_6H_CUSTOM_01 = templateMeta;
