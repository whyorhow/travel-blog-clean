import templateMeta from "../../../assets/filmstripTemplates/4HCustom02.json";

/**
 * Strip #2 — baked Illustrator chrome. Frame slots from green registration paths
 * in the SVG (see scripts/extract-filmstrip-template.js).
 */
export const FILMSTRIP_4H_CUSTOM_02 = templateMeta;
