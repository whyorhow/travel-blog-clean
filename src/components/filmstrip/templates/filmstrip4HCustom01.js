import templateMeta from "../../../assets/filmstripTemplates/4HCustom01.json";

/**
 * Strip #1 — baked Illustrator chrome. Frame slots from green registration paths
 * in the SVG (see scripts/extract-filmstrip-template.js).
 */
export const FILMSTRIP_4H_CUSTOM_01 = templateMeta;
